## `urban_platform.sdk` (namespace)

This namespace is reserved for SDK-facing surfaces. It intentionally avoids importing implementation modules at the package root.

See `docs/REPO_RESTRUCTURING_PLAN.md`.

