from __future__ import annotations

import pandas as pd


def register_roads(roads_gdf) -> pd.DataFrame:
    """
    Stub road registry. Will be wired when road entities become first-class.
    """
    return pd.DataFrame()

