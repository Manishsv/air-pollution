from __future__ import annotations

import pandas as pd


def register_buildings(buildings_gdf) -> pd.DataFrame:
    """
    Stub building registry. Will be wired when building entities become first-class.
    """
    return pd.DataFrame()

