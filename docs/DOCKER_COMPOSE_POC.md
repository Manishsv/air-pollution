# Docker Compose POC (historical stub)

This document is kept as a **historical compatibility stub** for older links.

Canonical docs:

- **Compose pilot runtime (Core API + dashboard)**: [`docs/DOCKER_COMPOSE_PILOT_RUNTIME.md`](DOCKER_COMPOSE_PILOT_RUNTIME.md)
- **Single-image Docker**: [`docs/DOCKER_DEPLOYMENT.md`](DOCKER_DEPLOYMENT.md)

Note: This repo’s Compose path is **pilot-runtime only** (not production-secure).

